services:
  app:
    image: python:3.12-slim
    working_dir: /workspace
    volumes:
      - ./:/workspace:cached
    command: bash -lc "pip install -U pip && pip install -r requirements.txt && python -m main"
    ports:
      - "8000:8000"
